# Stateful Audio Core ML Snippets

## Problem

Implement Swift snippets for an iOS 18+ stateful audio Core ML model that uses `MLState` across ordered audio frames and uses `MLTensor` for a small preprocessing step before creating the model input. Include concurrency rules, reset behavior, and the tensor materialization boundary.

Avoid unsupported or invented tensor APIs.

## Output

Create a Markdown file named `coreml-state-tensor.md`.
