# Large Core ML Model Deployment Note

## Problem

Write a deployment note for a 600 MB Core ML model in an iOS app. Compare bundling, Background Assets, On-Demand Resources, and server delivery. Include memory, background compute, compile/cache, and App Store size caveats.

Avoid stale download-limit claims.

## Output

Create a Markdown file named `coreml-deployment-assets.md`.
