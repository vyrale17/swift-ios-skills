A team asks for a Core Data plan, but the actual work is to run Core Data and SwiftData side by side against the same existing SQLite store, then move new screens to `@Model` over time.

Describe how the agent should route this request and give only the high-level boundary guidance that belongs in a Core Data answer.
