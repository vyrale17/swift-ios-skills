# SpeechAnalyzer Live Transcription

## Problem/Feature Description

An iOS team is building a meeting recorder for iOS 26. It needs live transcript
text while recording and word or phrase highlighting during playback.

Sketch the SpeechAnalyzer implementation shape, including model setup, audio
input, result handling, and cleanup.

## Output Specification

Create a file named `speech-recognition-speechanalyzer-live.md` containing:

- A Swift-oriented setup plan for `SpeechAnalyzer` and a transcriber module.
- Asset installation and locale/device support checks.
- The live audio input shape from `AVAudioEngine` to analyzer input.
- Result handling for live UI updates and playback timing.
- Cleanup/finalization guidance.

Do not create an Xcode project. Keep the answer as implementation guidance and
snippets only.
